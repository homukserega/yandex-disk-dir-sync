services:
  app:
    build: .
    restart: unless-stopped
    volumes:
      # Монтируем каталог из хоста в /app/data внутри контейнера
      - "${HOST_PATH:-./data}:/app/data:ro"
      - ./logs:/app/logs:rw
